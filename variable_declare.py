#!/usr/bin/env python3

# Types are inferred when declaring variables
num = 3  # python knows this value is int, as opposed to c and others
word = "text"  # python knows this value is string, and so on...
decimals = 2.30
character = 'c'

print(num, word, decimals, character)
