#!/usr/bin/env python3

# You can get type the type of an object in python code
# with the type(obj) function.
# 'obj' being variables and other objects.

aStr = 'hi'
aInt = 1

print( type(aStr) )
# remember to print(func) any object that simply
# returns data to get the ouput. 'type(aInt)' won't print
print( type(aInt) )
