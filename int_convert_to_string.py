#!/usr/bin/env python3

# int can easy be converted to string with the str() function
x = 3 # create a new int variable
print("for x, type is: ", type(x)) # show current type of variable
xstr = str(x) # # this converted x's type and is now new variable xstr
print("x's new type: ", type(xstr))
